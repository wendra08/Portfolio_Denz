<section
  id="home"
  class="relative min-h-[100svh] overflow-hidden bg-charcoal"
>
  <!-- ========================================
       BACKGROUND IMAGE
  ========================================= -->

  <div
    class="hero-background absolute inset-0 scale-[1.02] bg-cover"
    style="background-image: url('/images/hero-kang-denz.png');"
    aria-hidden="true"
  ></div>

  <!-- MOBILE DARK OVERLAY -->

  <div
    class="absolute inset-0 bg-black/25 md:bg-black/40"
    aria-hidden="true"
  ></div>

  <!-- LEFT / MOBILE GRADIENT -->

  <div
    class="absolute inset-0 bg-gradient-to-r from-charcoal via-charcoal/80 to-charcoal/10 md:via-charcoal/75"
    aria-hidden="true"
  ></div>

  <!-- BOTTOM GRADIENT -->

  <div
    class="absolute inset-0 bg-gradient-to-t from-charcoal via-charcoal/10 to-black/10"
    aria-hidden="true"
  ></div>

  <!-- GOLD GLOW -->

  <div
    class="pointer-events-none absolute -top-40 -left-40 h-[500px] w-[500px] rounded-full bg-gold/5 blur-[140px]"
    aria-hidden="true"
  ></div>

  <!-- ========================================
       MAIN CONTENT
  ========================================= -->

    <div
        class="container-luxury relative z-10 flex min-h-[100svh] items-start pt-28 pb-12 md:items-center md:pt-32 md:pb-24 lg:pb-28"
    >
    <div class="w-full max-w-6xl">

      <!-- SMALL LABEL -->

      <div
        class="hero-reveal hero-delay-1 flex items-center gap-3"
      >
        <span
          class="h-px w-8 bg-gold md:w-10"
        ></span>

        <p
          class="text-[8px] font-semibold tracking-[0.28em] text-gold uppercase sm:text-[9px] md:text-xs"
        >
          Professional Wedding MC
        </p>
      </div>

      <!-- NAME -->

      <p
        class="hero-reveal hero-delay-2 mt-4 text-[8px] tracking-[0.28em] text-ivory-muted uppercase sm:text-[9px] md:mt-8 md:text-xs md:tracking-[0.35em]"
      >
        Muhamad Denny Hidayat, S.Pd
      </p>

      <!-- MAIN TITLE -->

      <h1
        class="hero-reveal hero-delay-3 mt-2 font-serif text-[4.6rem] leading-[0.72] font-medium tracking-[-0.055em] sm:text-[5.4rem] md:mt-5 md:text-[clamp(6rem,12vw,11rem)]"
      >
        KANG <span class="block text-gold">
          DENZ
        </span>
      </h1>

      <!-- TAGLINE -->

      <div
        class="hero-reveal hero-delay-4 mt-8 max-w-[340px] border-l border-gold/50 pl-4 sm:max-w-md md:mt-14 md:max-w-3xl md:pl-8"
      >
        <h2
          class="font-serif text-[1.7rem] leading-[1.02] tracking-[-0.025em] sm:text-[2rem] md:text-[clamp(2.5rem,4vw,4rem)]"
        >
          More than just speaking. <span class="block text-ivory-muted">
            It's about understanding the moment.
          </span>
        </h2>
      </div>

      <!-- SERVICES -->

      <div
        class="hero-reveal hero-delay-5 mt-5 flex max-w-[340px] flex-wrap gap-x-3 gap-y-1 text-[9px] leading-5 tracking-[0.03em] text-ivory-muted sm:max-w-md md:mt-7 md:max-w-2xl md:text-sm"
      >
        <span>Wedding MC</span>
        <span class="text-gold">•</span>

        <span>Wedding Planner</span>
        <span class="text-gold">•</span>

        <span>Photography Management</span>
        <span class="text-gold">•</span>

        <span>MC Private Mentor</span>
      </div>

      <!-- CTA -->

      <div
        class="hero-reveal hero-delay-6 mt-7 flex w-full max-w-[340px] flex-col gap-3 sm:max-w-md sm:flex-row md:mt-10 md:max-w-none md:w-auto"
      >
        <a
          href="#booking"
          class="group inline-flex min-h-12 items-center justify-center gap-4 bg-gold px-6 py-3 text-[9px] font-bold tracking-[0.19em] text-charcoal uppercase transition-all duration-500 hover:bg-gold-light md:min-h-0 md:px-10 md:py-4 md:text-[10px]"
        >
          Book Kang Denz <span
            class="transition-transform duration-300 group-hover:translate-x-1"
          >
            →
          </span>
        </a>

        <a
          href="#portfolio"
          class="group inline-flex min-h-12 items-center justify-center gap-4 border border-white/20 px-6 py-3 text-[9px] font-semibold tracking-[0.19em] text-ivory uppercase transition-all duration-500 hover:border-gold hover:text-gold md:min-h-0 md:px-10 md:py-4 md:text-[10px]"
        >
          View Portfolio <span
            class="transition-transform duration-300 group-hover:translate-x-1"
          >
            →
          </span>
        </a>
      </div>

    </div>
  </div>

  <!-- ========================================
       SIDE TEXT - DESKTOP ONLY
  ========================================= -->

  <div
    class="absolute top-1/2 right-8 z-10 hidden -translate-y-1/2 xl:block"
  >
    <p
      class="vertical-text text-[9px] tracking-[0.35em] text-white/40 uppercase"
    >
      Wedding MC · Since 2017
    </p>
  </div>

    <!-- ========================================
       SCROLL INDICATOR
  ========================================= -->

  <a
    href="#about"
    class="absolute bottom-8 left-1/2 z-20 hidden -translate-x-1/2 flex-col items-center gap-3 lg:flex"
    aria-label="Scroll to About section"
  >
    <span
      class="text-[8px] tracking-[0.35em] text-white/40 uppercase"
    >
      Scroll
    </span>

    <span
      class="scroll-line h-10 w-px bg-gradient-to-b from-gold to-transparent"
    ></span>
  </a>

</section>

<style>
  .hero-background {
    background-position: 67% center;
  }

  @media (min-width: 640px) {
    .hero-background {
      background-position: 65% center;
    }
  }

  @media (min-width: 1024px) {
    .hero-background {
      background-position: center;
    }
  }
</style>